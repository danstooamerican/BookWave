<Page x:Class="Commons.Pages.AddPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Commons.Pages"
      xmlns:lib="clr-namespace:Commons.Controls;assembly=Commons.Controls" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AuthorsPage"
      DataContext="{Binding AddPageViewModel, Source={StaticResource Locator}}">
    
    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Style="{StaticResource CustomScrollViewer}">
        <Border Padding="20, 0, 20, 0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Add New Title -->
                <Border Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2"
                    Padding="0, 10, 0, 20">
                    <StackPanel>
                        <TextBlock  
                    HorizontalAlignment="Left" VerticalAlignment="Bottom"
                    Style="{StaticResource InformationTextHighlight}" Text="Add New:" 
                    TextTrimming="WordEllipsis" FontSize="35" />
                        <Border Style="{StaticResource HorizontalSeparator}" />
                    </StackPanel>
                </Border>

                <!-- Title -->
                <TextBlock  Grid.Column="0" Grid.Row="1"
                    VerticalAlignment="Center" HorizontalAlignment="Right"
                    Margin="0, 10, 0, 0"
                    Style="{StaticResource InputMaskText}" Text="Title:" 
                    TextTrimming="WordEllipsis" />

                <TextBox    Grid.Column="1" Grid.Row="1"
                    Margin="10,10,10,0" MinWidth="400"
                    VerticalAlignment="Center" HorizontalAlignment="Left"
                    Style="{StaticResource MaskTextBox}"
                     />

                <!-- Drag Area -->
                <DataGrid Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2"
                        ItemsSource="{Binding Chapters}" AutoGenerateColumns="False"
                        Margin="30" RowHeight="30" ColumnWidth="100" />
            </Grid>
        </Border>       
    </ScrollViewer>
    
</Page>
