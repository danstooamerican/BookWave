<src:DialogWindow x:Class="BookWave.Desktop.AudiobookManagement.Dialogs.SelectLibraryItemDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:src="clr-namespace:BookWave.Desktop.AudiobookManagement.Dialogs"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lib="clr-namespace:BookWave.Controls;assembly=BookWave.Controls" 
        mc:Ignorable="d"
        Icon="/BookWave.Styles;component/Resources/icon.ico"
        WindowStyle="None"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        Background="{StaticResource Background2ColorBrush}"
        BorderThickness="1"
        BorderBrush="{StaticResource TextHighlightColorBrush}"
        WindowStartupLocation="CenterOwner"
        Title="Select Audiobook" Height="400" Width="600">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <lib:ImageButton Grid.Column="0" Grid.Row="0"
            NormalImage="/BookWave.Styles;component/Resources/Navigation/close.png"
            HoverImage="/BookWave.Styles;component/Resources/Navigation/close_pressed.png"
            PressedImage="/BookWave.Styles;component/Resources/Navigation/close_pressed.png"
            ImageSize="20"
            HorizontalAlignment="Right"
            IsCancel="True"
            Margin="5"
            WindowChrome.IsHitTestVisibleInChrome="True"/>

        <!-- Library Selection -->
        <StackPanel Orientation="Horizontal" Grid.Column="0" Grid.Row="1"
            Margin="27, 10">
            <TextBlock
                VerticalAlignment="Center"
                Style="{StaticResource InputMaskText}" Text="Library:" 
                TextTrimming="WordEllipsis" />

            <ComboBox Margin="10,0"
                MinWidth="300"
                VerticalAlignment="Center"
                ItemsSource="{Binding Libraries}"
                Style="{StaticResource ComboBoxStyle}"
                ItemTemplate="{StaticResource LibraryContentTemplate}"
                SelectedItem="{Binding Library}" />

            <Button Style="{StaticResource RoundedButton}" Content="Scan Library"
                Command="{Binding ScanLibraryCommand}"/>
        </StackPanel>

        <ListView Grid.Column="0" Grid.Row="2" 
            VirtualizingStackPanel.IsVirtualizing="True"
            BorderBrush="Transparent" Background="Transparent"
            VerticalContentAlignment="Top" HorizontalContentAlignment="Left"
            ScrollViewer.HorizontalScrollBarVisibility="Hidden"
            ItemsSource="{Binding Audiobooks}"
            SelectedItem="{Binding Selected}"
            Padding="20" ItemContainerStyle="{StaticResource LibraryItemStyle}"
            MouseDoubleClick="ListView_MouseDoubleClick">

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Width="{Binding Path=ActualWidth, 
                        RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListView}}" 
                            Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
    </Grid>

</src:DialogWindow>
