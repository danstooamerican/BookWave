<Window x:Class="Commons.Dialogs.SelectLibraryItemDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Commons.Dialogs"
        mc:Ignorable="d"
        Icon="/Commons.Styles;component/Resources/icon.ico"
        WindowStyle="None"
        AllowsTransparency="True"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        WindowStartupLocation="CenterOwner"
        Title="SelectLibraryItemDialog" Height="400" Width="600">

    <Border BorderBrush="{StaticResource Background1ColorBrush}" BorderThickness="1">
        <Grid Background="{StaticResource Background2ColorBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <Button Grid.Column="1" Grid.Row="1" MinHeight="28" MinWidth="120" MaxWidth="130"
                Margin="0, 0, 10, 5"
                Content="Cancel" IsCancel="True" Style="{StaticResource RoundedButton}" />
            
            <Button Grid.Column="2" Grid.Row="1" MinHeight="28" MinWidth="120" MaxWidth="130"
                Margin="0, 0, 5, 5" Click="Select_Click"
                Content="Select" IsDefault="True" Style="{StaticResource RoundedButton}" />
        </Grid>
    </Border>
    
</Window>
