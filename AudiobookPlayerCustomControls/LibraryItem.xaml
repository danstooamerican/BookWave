<UserControl x:Class="Commons.Controls.LibraryItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Commons.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Image x:Name="imgCover" Grid.Row="0" MinHeight="{Binding MinWidth, ElementName=imgCover}"
               Source="{Binding CoverImage}">
            
        </Image>

        <TextBlock Grid.Row="1" 
            Style="{StaticResource InformationTextHighlight}" Text="{Binding Title}" />
        
        <TextBlock Grid.Row="2"
            Style="{StaticResource InformationText}" Text="{Binding Description}" />
    </Grid>
</UserControl>
